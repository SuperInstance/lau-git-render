[package]
name = "lau-git-render"
version = "0.1.0"
edition = "2021"
description = "Rendering layer for git-native agents — A2A/A2UI bridge"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
